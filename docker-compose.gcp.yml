##
# docker-compose.gcp.yml
#
# Usage:
#   export DOCKER_IMAGE_TAG=$(git rev-parse --short HEAD)
#   docker-compose -f docker-compose.yml -f docker-compose.gcp.yml
##
version: '3.4'
services:
 node1:
   image: gcr.io/harp-angell-utils/node1
 node2:
   image: gcr.io/harp-angell-utils/node2
 dashboard:
   image: gcr.io/harp-angell-utils/dashboard
 ethnet:
   image: gcr.io/harp-angell-utils/ethnet
